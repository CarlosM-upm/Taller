# /etc/nut/nut.conf
#
# El SAI está enchufado por USB al propio mini-PC y no lo vigila nadie más:
# "standalone" (driver + servidor + monitor en la misma máquina).
#
# SIN PROBAR: esta configuración se ha escrito sin tener el SAI delante.
# Los pasos para ajustarla y comprobarla están en DESPLIEGUE.md.
MODE=standalone
